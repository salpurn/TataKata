[build]
builder = "NIXPACKS"

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"

cmd = "php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"

[env]
APP_ENV = "production"
APP_DEBUG = "false"
APP_URL = "https://tatakata.up.railway.app"
